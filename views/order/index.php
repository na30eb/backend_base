<?php

use yii\helpers\ArrayHelper;

echo "kfhdsiofhds";


$options = ArrayHelper::map($options, 'name', 'family');
var_dump($options);

//foreach ($options as $option) {
//    //find the id of brands and use them as an index and then save their name in the sam index
//   echo $modeldropdownOptions[$option->id] = $option->name ." ". $option->family." | id= ". $option->id;
//}